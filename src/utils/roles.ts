export const FEATURES = {
  ADMIN: 'admin',
  USER: 'user',
  SUBSCRIPTION:'subscription'
};

export const ACCESS_TYPES = {
  READ: true,
  CREATE: true,
  UPDATE: true,
  DELETE: true,
};

export const ROLES = {
    ADMIN:"admin",
    USER:"user"
}